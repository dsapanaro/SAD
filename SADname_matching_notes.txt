
establish match between main name field and FRESCO subject headings [where can i get access to the SH list? scrape subject headings from FRESCO?]
goal: match two flat files (CSV)
insert FRESCO subject heading into LCN field

establish match between main name and VIAF
insert VIAF name contributed from LC into LCN field. if no LC name, insert ULAN contributed name into a new field called ULAN. 
goal: call an API and match names

match means determine which ones might be the same
want to use functions (define in a separate .py file and import)